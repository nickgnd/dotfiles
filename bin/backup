#!/usr/bin/env bash
#
# backup
#
# `backup` performs daily backups of specified directories

set -o errexit
set -o nounset
set -o pipefail

readonly DOTFILES="$HOME/dotfiles"

"$DOTFILES/backup/backup.sh"
